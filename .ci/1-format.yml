# Format:
#   stage: Format
#   cache:
#     key: "$CI_JOB_NAME-$CI_COMMIT_REF_SLUG"
#   extends:
#     - .job-base
#     - .docker-linux-runner
#   script:
#     - yarn
#     - yarn linkdist
#     - yarn format
#     - yarn lint || true
#     - git diff-index --quiet HEAD || git commit -am "format & fix" && git push
