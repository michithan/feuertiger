Format:
  extends:
    - .job-base
    - .docker-linux-runner
  stage: Format
  script:
    - yarn linkdist
    - yarn format
    - yarn lint || true
    - git diff-index --quiet HEAD || git commit -am "format & fix" && git push
