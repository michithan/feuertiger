pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - approved-reviews-by>=1
      - author=dependabot-preview[bot]
      - status-success=Test
    actions:
      merge:
        method: squash
        strict: smart
