{
  "root": "apps/feuertiger-web",
  "sourceRoot": "apps/feuertiger-web",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/feuertiger-web",
        "outputPath": "dist/apps/feuertiger-web"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "feuertiger-web:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "feuertiger-web:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "feuertiger-web:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/feuertiger-web"],
      "options": {
        "jestConfig": "apps/feuertiger-web/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/feuertiger-web/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
