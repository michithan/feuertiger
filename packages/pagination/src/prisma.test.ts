import { test, describe, expect } from '@jest/globals';

describe('Test prisma pagination utils', () => {
    test('should create connection resolver', () => {
        expect(true).toBeTruthy();
    });
});
