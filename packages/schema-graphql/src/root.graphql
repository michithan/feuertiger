# import * from "directives.graphql"
#
# import * from "base.inputs.graphql"
# import * from "base.interfaces.graphql"
# import * from "base.types.graphql"
# import * from "base.scalars.graphql"
#
# import * from "person.type.graphql"
# import * from "exercise.type.graphql"

##############
# Root Query #
##############

type Query {
    node(id: ID!): Node
    allPersons(filter: Filter): PersonConnection
    allExercises(filter: Filter): ExerciseConnection
    ocr(image: String): String
}

type Mutation {
    delete(id: ID!): Boolean
}
