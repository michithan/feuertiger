type Membership implements Node {
    id: ID!
    person: Person
    active: Boolean
    entryDate: DateTime
    departureDate: DateTime
}

input MembershipUpdate {
    id: ID!
    active: Boolean
    entryDate: DateTime
    departureDate: DateTime
}
