type Membership implements Node {
    id: ID!
    person: Person
    active: Boolean
    entryDate: String
    departureDate: String
}

input MembershipUpdate {
    id: ID!
    active: Boolean
    entryDate: String
    departureDate: String
}
