enum UserRole {
    GUEST
    ADMIN
}

type Viewer implements Node {
    id: ID!
    uid: ID!
    email: String
    userRoles: [UserRole]
    person: Person
}

extend type Mutation {
    connectUserToPerson(userId: ID!, personId: ID!): Viewer
}
