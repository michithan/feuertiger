enum UserRole {
    USER
    ADMIN
}

type User implements Node {
    id: ID!
    uid: ID!
    email: String!
    userRoles: [UserRole!]
    person: Person
    openMembershipRequest: MembershipRequest
}

extend type Mutation {
    connectUserToPerson(userId: ID!, personId: ID!): User
}
