import React, { ReactNode } from 'react';

type Props = {
    children?: ReactNode;
    index: number;
    value: number;
};

export default class HomePage extends React.Component<Props> {
    render() {
        return null;
    }
}
