name: feuertiger-deployments
runtime: nodejs
description: feuertiger deployments
template:
    config:
        gitlab:user:
            description: gitlab user
        gitlab:token:
            description: gitlab token
